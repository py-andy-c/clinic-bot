[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

# Note: Railpack may prefer Procfile over nixpacks.toml start command
# If Procfile exists, it will be used instead
[start]
cmd = "bash start.sh"

